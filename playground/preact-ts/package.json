{
  "name": "preact-ts",
  "private": true,
  "version": "0.1.0-dev",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./dist",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@types/node": "^20.4.7",
    "shrtcss": "workspace:*",
    "typescript": "^5.1.6"
  }
}
