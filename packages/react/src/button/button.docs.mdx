---
title: Docs
---

import Button from './button'
import Icon from '../icon/icon'
import Spinner from '../spinner/spinner'

# Button

[Button](https://www.w3.org/WAI/ARIA/apg/patterns/button/) allows users to perform actions.

## Color Variants

<div className='example-wrapper wrap'>
  <Button>Button</Button>
  <Button color={'info'}>Button</Button>
  <Button color={'success'}>Button</Button>
  <Button color={'warning'}>Button</Button>
  <Button color={'danger'}>Button</Button>
  <span style={{flexBasis: '100%', height: '0'}} />
  <Button variant={'outline'}>Button</Button>
  <Button variant={'outline'} color={'info'}>Button</Button>
  <Button variant={'outline'} color={'success'}>Button</Button>
  <Button variant={'outline'} color={'warning'}>Button</Button>
  <Button variant={'outline'} color={'danger'}>Button</Button>
  <span style={{flexBasis: '100%', height: '0'}} />
  <Button variant={'light'}>Button</Button>
  <Button variant={'light'} color={'info'}>Button</Button>
  <Button variant={'light'} color={'success'}>Button</Button>
  <Button variant={'light'} color={'warning'}>Button</Button>
  <Button variant={'light'} color={'danger'}>Button</Button>
</div>

## Icon

<div className='example-wrapper wrap'>
  <Button leftIcon={'ps4'} aria-label='Icon-only Button' />
  <Button leftIcon={'info'} variant={'filled'} color={'info'} aria-label='Icon-only Button' />
  <Button leftIcon={'success'} variant={'filled'} color={'success'} aria-label='Icon-only Button' />
  <Button leftIcon={'question'} variant={'filled'} color={'warning'} aria-label='Icon-only Button' />
  <Button leftIcon={'danger'} variant={'filled'} color={'danger'} aria-label='Icon-only Button' />
</div>

## Icon & Label

<div className='example-wrapper wrap'>
  <Button leftIcon={'expand'}>
    <span>Expand</span>
  </Button>
  <Button rightIcon={'settings'}>
    <span>Settings</span>
  </Button>
  <Button leftIcon={'flash'} rightIcon={'arrowRight'}>
    <span>Open Stackblitz</span>
  </Button>
</div>

## Loading Spinner

<div className='example-wrapper wrap'>
  <Button><Spinner size={'xs'} /></Button>
  <Button disabled>
    <Spinner size={'xs'} />
    <span>Loading</span>
  </Button>
</div>

## Size Variants

<div className='example-wrapper wrap'>
  <Button size={'xl'}>X-Large</Button>
  <Button size={'lg'}>Large</Button>
  <Button>Medium</Button>
  <Button size={'sm'}>Small</Button>
  <Button size={'xs'}>X-Small</Button>
</div>

## Disabled

<div className='example-wrapper wrap'>
  <Button disabled>Button</Button>
  <Button variant={'filled'} color={'info'} disabled>Button</Button>
  <Button variant={'filled'} color={'success'} disabled>Button</Button>
  <Button variant={'filled'} color={'warning'} disabled>Button</Button>
  <Button variant={'filled'} color={'danger'} disabled>Button</Button>
</div>

## Full-Width

<div className='example-wrapper wrap'>
  <Button fullWidth>Button</Button>
</div>
