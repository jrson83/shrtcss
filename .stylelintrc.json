{
  "extends": [
    "stylelint-config-standard-scss",
    "stylelint-config-property-sort-order-smacss"
  ],
  "rules": {
    "custom-property-pattern": [
      "^(_)?([a-z][a-z0-9]*)(-[a-z0-9]+)*$",
      {
        "message": "Expected custom property name to be kebab-case, with an optional `_` prefix"
      }
    ]
  }
}
